# =============================================================================
# Backend configuration file for HA Load Balancer
# Mount to: /etc/haproxy-lb/backends.conf
#
# One backend per line: IP:PORT
# Lines starting with # are comments (also strips inline comments)
# This file takes precedence over BACKENDS_LIST environment variable
# =============================================================================
#
# Example service nodes — update IPs and ports for your service
#
192.168.1.10:80
192.168.1.11:80
192.168.1.12:80
