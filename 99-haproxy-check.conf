# =============================================================================
# Drop-in for keepalived.service — ensures it starts AFTER haproxy
# Install: systemctl edit keepalived.service
#          Then paste this content
# =============================================================================
#
# [Unit]
# After=haproxy.service
# Requires=haproxy.service
#
# [Service]
# # Reload keepalived when haproxy restarts (triggers re-check)
# ExecReload=/bin/kill -HUP $MAINPID
#
